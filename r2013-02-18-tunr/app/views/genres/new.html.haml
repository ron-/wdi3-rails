.row
  .four.columns
    = render(:partial => 'shared/errors', :locals => {:object => @genre})
    %h4 Add genre:
    = form_for @genre do |f|
      = f.label :name
      = f.text_field :name, :autofocus => true;
      = f.submit nil, :class => 'button radius'

      - Song.order(:name).each do |song|
        = song.name
        = check_box_tag 'genre[song_ids][]', song.id, song.in?(@genre.songs.to_a)

  .eight.columns
