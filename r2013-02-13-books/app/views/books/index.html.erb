<% @books.each do |book| %>
  <div class='bigimg'>
    <%= image_tag(book.image) %>
  </div>
  <h1><%= link_to book.title, book_path(book.id) %></h1>
  <ul>
    <li><%= book.pubdate %></li>
    <li><%= book.publisher %></li>
    <li><%= book.pages %></li>
  </ul>
<% end %>